@{
    ViewData["Title"] = "Manager Dashboard";
    var username = Context.Session.GetString("Username") ?? "Manager";
}

<div class="container mt-5 text-center">
    <h2 class="mb-3">Welcome, @username /h2>
    <p class="text-muted">As a manager, you have oversight of all lecturer claims, with the ability to review, approve, or reject them.</p>

    <a href="/Manager/ClaimsList" class="btn btn-primary btn-lg shadow-sm">View All Claims</a>
</div>
